{
  "name": "@imgopt/maintenance",
  "version": "0.0.0",
  "private": true,
  "description": "EventBridge-scheduled Lambda: orphan reconciliation, superseded-version expiry, pending-upload reaping, storage accounting.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "clean": "tsc --build tsconfig.build.json --clean",
    "bundle": "node esbuild.mjs",
    "test": "vitest run",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  },
  "dependencies": {
    "@imgopt/config": "workspace:*",
    "@imgopt/core": "workspace:*",
    "@imgopt/db": "workspace:*",
    "@imgopt/metrics": "workspace:*",
    "@imgopt/storage": "workspace:*",
    "pino": "^9.14.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "esbuild": "^0.24.0",
    "vitest": "^2.1.8"
  }
}
