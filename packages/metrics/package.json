{
  "name": "@imgopt/metrics",
  "version": "0.0.0",
  "private": true,
  "description": "CloudWatch Embedded Metric Format emission, shared by the API and both Lambdas.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "clean": "tsc --build tsconfig.build.json --clean",
    "test": "vitest run"
  },
  "dependencies": {
    "@imgopt/core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
